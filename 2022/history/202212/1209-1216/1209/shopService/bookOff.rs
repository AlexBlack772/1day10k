use std::io;

struct Person {
    name: String,
    age: i32,
    thing: str[],
    price: i32,
}

struct book {
    name: String,
    price: i32,
}

fn kaitori (price: i32) -> i32 {
   if Person.money < price {
       println!("You don't have enough money");
   } else {
         Person.money = Person.money - price;
         
   }
}

fn hanbai (price: i32) -> i32 {
   if Person.money < price {
       println!("You don't have enough money");
   } else {
         Person.money = Person.money - price;
         
   }
}

fn neduke (price: i32) -> i32 {
   if Person.money < price {
       println!("You don't have enough money");
   } else {
         Person.money = Person.money - price;
         
   }
}

fn 
fn main () {}