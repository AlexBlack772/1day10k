//modules borrowとは、他のモジュールの要素を使うことを意味する。
use std::borrow::Borrow;
//processとは、プロセスのことです。
use std::process;
//syncとは、同期のことです。
use std::sync;
//threadとは、スレッドのことです。
use std::thread;
//assertとは、アサートのことです。
use std::assert;
//assert_eqとは、アサートイコールのことです。
use std::assert_eq;
//traitとは、トレイトのことです。
use std::trait;
//typeとは、型のことです。
use std::type;
//ptrとは、ポインタのことです。
use core::ptr;
//memとは、メモリのことです。
use core::mem;
//cmpとは、比較のことです。
