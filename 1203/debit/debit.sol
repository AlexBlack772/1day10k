pragma solidity ^0.8.13;

contract debit {
      uint public profit;

      event Rishi();
/*
      function rishi() public payable {
         require(msg.value == rishi);
         msg.sender(msg.value);

         emit Rishi(msg.sender, rishi);
      }

      function kariru(uint rishi) public payable {
         require(msg.value == rishi);
         msg.sender(msg.value);

         emit Rishi(msg.sender, rishi);
      }
*/
}