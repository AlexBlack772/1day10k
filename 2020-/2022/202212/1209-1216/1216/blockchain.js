class block {
   constructor(){
      this.block 
      this.
   }
}

const a = TextDecoderStream

const test = new a(a, a)

const test2 = new test(test, test)

class blockchain{
   constructor(){
      this.block = new block();
   }

   getBlock() {
      return this.block;
   }

   setBlock(block) {

      this.block = block;
   }


}

const blockchain = new blockchain();

const block = blockchain.getBlock();

blockchain.setBlock(block);

function block() {
   this.block = block;
}

const block = new block();

class blockchain {
   constructor() {
      this.block = new block();
   }

   getBlock() {
      return this.block;
   }

   setBlock(block) {
      this.block = block;
   }
}

try {
   const blockchain = new blockchain();
   const block = blockchain.getBlock();
   blockchain.setBlock(block);
}
catch (e) {
   console.log(e);
}

function block() {
   this.block = block;
}

let block = new block();

for (let i = 0; i < 100; i++) {
   block = new block();
}

function block() {
   this.block = block;
}

for (let i = 0; i < 100; i++) {
   let block = new block();
}

it('should be able to create a new block', () => {
   const block = new block();
   expect(block).to.be.an.instanceof(block);
}
)

Object(blockchain).block = new block();

const block = new block();

Object(blockchain).block = block;

var block = new block();

while (true) {
   block = new block();
}

debugger;

var block = new block();

balance++;

if (a && b) {
   balance++;
} else if (a || b) {
   balance++;
}else if (a == b) {
   balance++;
} else if (a != b) {
   balance++;
}

if (a == b) {
   balance++;
}else if (a != b) {
   balance++;
} else if (a < b) {
   balance++;
} else if (a !== b) {
   balance++;
}

if (a == b) {
   balance++;
} else if (a != b) {
   balance++;
}

if (typeof 'string') {
   balance++;
} else if (typeof 1) {
   balance++;
}

if (void 0) {
   balance++;
}

if (instanceof block) {
   balance++;
}

parseInt('1');
parseFloat('1');

Number('1');
String(1);
Boolean(1);

boolean = new Boolean(1);
string = new String(1);
number = new Number(1);

var block = new block();

var map = new Map();

map.set('key', 'value');
map.get('key');
map.get('key2');

map.forEach((value, key) => {
   console.log(value, key);
})

var set = new Set();

map.has('key');

map.delete('key');
map.clear();

map.size;

var block = new block();

set.add('value');

var set = new Set();

console.log(set.has('value'));

var set = new Set();
set.size;

set.keys();

set.values();
set.entries();

set.forEach((value, key) => {
   console.log(value, key);
})

var obj = {
   key: 'value'
}

var obj = new Object();
var obj = Object.create(null);

var obj = {
   width: 100,
   height: 100,
}
console.log(obj.width);
console.log(obj.height);

function childObject() {
   this.width = 100;
   this.height = 100;
}

childObject.protype = Object.create(parentObject.prototype);
console.log(childObject.width);

var obj = Object.create(null);

Date.protype.tznama = function () {
   return 'Asia/Seoul';
}

var date = new Date();

Date.protype.toString = function () {
   return 'Asia/Seoul';
}

var date = new Date();

var proto = {
   hello
}

var obj = Object.create(proto);

var obj = {
   hello
}

var obj = { x: 100, y: 100 };
console.log(obj.__proto__ === Object.prototype);

var obj = { x: 100, y: 100 };
console.log(Object.value(obj));

var obj = { x: 100, y: 100 };
console.log(Object.keys(obj));

var names = Object.getOwnPropertyNames(obj);

var obj = {}
obj[Symbol('key')] = 'value';
obj[Symbol('key')] = 'value';

var symbols = Object.getOwnPropertySymbols(obj);

console.log(symbols);

var obj = {
   [Symbol('key')]: 'value',
   [Symbol('key')]: 'value'
}

console.log(obj.hasOwnProperty(Symbol('key')));

console.log(Object.hasOwn(obj, Symbol('key')));

console.log(arr.propertyIsEnumerable('length'));

console.log(Object.isExtensible(obj));

console.assert(1 === 2, '1 is not equal to 2');

console.count('count');

console.clear();

console.dir(obj);

console.log(JSON.parse(JSON.stringify(obj)));
console.debug(obj);
console.debug(JSON.stringify(obj));

console.error('error');
console.info('info');

console.warn('warn');

console.error('error');

console.trace('trace');

console.time('time');

console.time('time');

console.timeEnd('time');
console.timeStart('time');

console.timeLog('time');

console.timeEnd('time');

console.timeLog('time');

console.groupCollapsed('group');

console.groupEnd('group');

console.group('group');

console.assert(1 === 2, '1 is not equal to 2');

console.clear();

console.dir(obj);

console.log(JSON.parse(JSON.stringify(obj)));

console.dirxml(obj);

console.table([obj, obj]);

function aFunci() {
   setTimeout(() => {
      callback(data * 2);
      console.log('aFunci');
   }, Math.random() * 1000);
         
}

function bFunci() {
   setTimeout(() => {
      callback(data * 2);
      console.log('bFunci');
   }, Math.random() * 1000);
}

function cFunci() {
   return new Promise((resolve, reject) => {
      setTimeout(() => {
         resolve(data * 2);
         console.log('cFunci');
      }, Math.random() * 1000);
   }
)}


function dFunci() {
   return new Promise((resolve, reject) => {
      setTimeout(() => {
         resolve(data * 2);
         console.log('dFunci');
      }, Math.random() * 1000);
   }
   )
}

var obj = { x: 100, y: 100 };

var str = JSON.stringify(obj);

var obj = JSON.parse(str);

var str = JSON.string(obj);

var obj = JSON.parse(str);

var obj = JSON.parse(str);

var obj = JSON.parse(str);

console.log(obj);

var obj = JSON.parse(str);
console.log(eval('"use strict";' + str));


console.log(arr.)

encodeURI('http://www.naver.com');

decodeURI('http://www.naver.com');

encodeURIComponent('http://www.naver.com');

function funci(a, b, c, ...rest) {
   console.log('funci');
}

add = (a, b) => {
   return a + b;
}

add = new Function('a', 'b', 'return a + b');

var obj = {
   add: function (a, b) {
      return a + b;
   }
}

this.name = 'global';

var module = {
   name: 'module',
   funci: function () {
      console.log(this.name);
   }
}

   (function () {
      console.log(this.name);

   })

   (function () {
   })

var obj = {
   name: 'obj',
   funci: function () {
      console.log(this.name);
   }
}

   (arg1, arg2) => {
      
}
   
class Person {
   constructor(name) {
      this.name = name;
   }
}

class Animal {
   constructor(name) {
      this.name = name;
   }
}

class Person extends Animal {
   constructor(name) {
      super(name);
   }

   say() {
      console.log('hello');
   }

   static say() {
      console.log('hello');
   }



}

class Animal {
   type = 'animal';
   constructor(name) {
      this.name = name;
   }

   say() {
      console.log('hello');
   }
}

class Person extends Animal {
   constructor(name) {
      super(name);
   }

   static say() {
      console.log('hello');
   }

   static msg = 'hello';

   static say() {
      console.log(this.msg);
   }

}

class Animal{
   get type() {
      return 'animal';
   }

   set type(value) {
      console.log('set type');
   }

}

class Person extends Animal {
   constructor(name) {
      super(name);
   }

   get type() {
      return 'person';
   }

   set type(value) {
      console.log('set type');
   }

}

xx = new Person('xx');

xx.type = 'person';

class Animal {
   constructor(name) {
      this.name = name;
   }

   say() {
      console.log('hello');
   }
}

if (true) {
   let name = 'xx';
}


var name = 'xx';

if (true) {
   var name = 'xx';
}

var name = 'xx';

if (true) {
   let name = 'xx';
}

for (let i = 0; i < 10; i++) {
   console.log(i);
}

for (let i = 0; i < 10; i++) {
   console.log(i);
}

for (let i = 0; i < 10; i++) {
   for(let i = 0; i < 10; i++) {
      console.log(i);
   }
}

for (let i = 0; i < 10; i++) {
   if (true) {
      let i = 0;
      console.log(i);


   }
}

for (let i = 0; i < 10; i++) {
   if (true) {
      let i = 0;
      console.log(i);
   }
}

xx += 1;
xx %= 2;

x <<= 1;

x >>= 1;

x >>>= 1;

x ??= 1;
xx ??= 1;

x &&= 1;

delete obj.x;

type(undefined);
typeof (undefined);
typeof (true);

typeof (1);

void function () {
   console.log('hello');
}

void 0;

if (true) {
   console.log('hello');
}

Animal.prototype.type = 'animal';
Animal.prototype.say = function () {
   console.log('hello');
}

class Animal {

}

class Person extends Animal {

}


class Animal {
   constructor(name) {
      this.name = name;
   }

   say() {
      console.log('hello');
   }
}

Object.defineProperties(Animal.prototype, {

   type: {
      value: 'animal',
      writable: true,
      enumerable: true,
      configurable: true
   },

   say: {
      value: function () {
         console.log('hello');
      }
   }
})


Object.defineProperties(Animal.prototype, {
})

Object.defineProperty(obj, 'x', {
   value: 100,
   writable: true,
   enumerable: true,
   configurable: true
})
   
Object.defineProperty(obj, 'x', {
   value: 100,
   writable: true,
   enumerable: true,
   configurable: true
})

Object.freeze(obj);

'use strict';

Object.defineProperty(obj, 'x', {
   value: 100,
   writable: true,
   enumerable: true,
   configurable: true
})

Object.seal(job);

obj['x'] = 100;
delete obj['x'];

Object.defineProperty(obj, 'x', {
   value: 100,
   writable: true,
   enumerable: true,
   configurable: true
})

Object.preventExtensions(obj);

obj['x'] = 100;

Object.defineProperty(obj, 'x', {
   value: 100,
   writable: true,
   enumerable: true,
   configurable: true
})

console.log(obj.hasOwnProperty('x'));

console.log(obj.propertyIsEnumerable('x'));

console.log(obj.isPrototypeOf(obj));

Object.toString();

Object.prototype.toString.call(obj);

Object.prototype.toString.call(obj);

console.log(obj.toString());

console.log(obj.valueOf());

console.log(date.toLocaleString());

console.log(date.toLocaleDateString());


class Cat extends Animal {
   constructor(name) {
      super(name);
   }
   this.name = name;
   
}


class CatA extends Animal {
   constructor(name) {
      super(name);
   }
   this.name = name;
   
}

console.log(date.toLocaleString())

console.log(date.toLocaleDateString())

console.log(obj.constructor);

console.log(obj.constructor.prototype);

Object.assign(obj, {
   x: 100,
   y: 200
})
   
Object.assign(obj, {
   x: 100,
   y: 200
})

Object.is(obj, obj);

console.info('hello');

console.assert(1 === 2, '1 is not equal to 2');

console.debug('hello');

Object.is(obj, obj);

set.add(1);

set.forEach(function (value) {
   console.log(value);
}
)
map.forEach(function (value, key) {
   console.log(value, key);
})
   
map.forEach(function (value, key) {
   console.log(value, key);
})

set.forEach(function (value) {
   console.log(value);
})

Math.abs(-1);

Array.bind();

Array.prototype.concat();

Array.prototype.copyWithin();

Date.apply();

Date.prototype.getDate();

Array.from();

Array.isArray();

Array.of();

Array.prototype.pop();

Array.prototype.push();

Date.toString();

Date.now();

Date.parse();

Object.values();

Object.keys();

Object.entries();

Object.assign();

Object.create();

Object.defineProperty();

Object.defineProperties();

Object.freeze();

Object.getOwnPropertyDescriptor();

event.composedPath();

event.preventDefault();

event.stopPropagation();

event.stopImmediatePropagation();

event.target();

event.stopPropagation();

event.stopImmediatePropagation();

isFinite();

isNaN();

Number.bind();

String.bind();

String.prototype.charAt();

String.raw();

String.prototype.concat();

String.bind();

Date.parse();

Date.now();

Number.MAX_VALUE;

Number.MIN_VALUE;

Number.NaN;

Number.NEGATIVE_INFINITY;

Number.POSITIVE_INFINITY;

Number.prototype.toExponential();

Number.prototype.toFixed();

Number.prototype.toLocaleString();

Date.UTC();

on('click', function () {
   console.log('hello');
}
)

on('click', function () {
   console.log('hello');
}
)

window.AudioContext = window.AudioContext || window.webkitAudioContext;

document.ATTRIBUTE_NODE.toExponential();

onload = function () {
   console.log('hello');
}

document.getElementsByName('name');

document.getSelection();

document.querySelector('div');

document.querySelectorAll('div');
event.composedPath();

event.preventDefault();

onclick = function () {
   console.log('hello');
}

object.addEventListener('click', function () {
   console.log('hello');
}
)  
object.addEventListener('click', function () {
   console.log('hello');
}
)

onclick = function () {
   console.log('hello');
}

object.addEventListener('click', function () {
   console.log('hello');
}

)

onkeydown = function (event) {
   console.log(event.keyCode);
}

document.getElementsByName('name');


form.ATTRIBUTE_NODE.toExponential();

hadleEvent = function (event) {
   console.log(event.keyCode);
}

document.getElementsByName('name');

window.navigator.userAgent;

appName;

Number.parseInt()

Number.parseFloat()

unescape()

decodeURI(encodeURI())

decodeURIComponent(encodeURIComponent())

eval()

Function()

Object()

function Person(name) {
   this.name = name;
}

Person.prototype.getName = function () {
   return this.name;
}

function Person(name) {
   this.name = name;
}

Person.call(this, name);

Person.prototype.getName = function () {
   return this.name;
}

function Person(name) {
   this.name = name;
}

Person.call(this, name);

Person.prototype.getName = function () {
   return this.name;
}

prototype.ATTRIBUTE_NODE.toExponential();

function Person(name) {
   this.name = name;
}

Element.apply(this, arguments)

Element.prototype.getName = function () {
   return this.name;
}
Element.prototype.append = function () {
   return this.name;
}

Element.name.charAt();

Element.name.concat();

Element.arguments();

Element.arguments.caller();

Element.chiNodes();
Element.parseInt()

location.hash;

document.ATTRIBUTE_NODE.toExponential();

document.COMMENT_NODE.toExponential(); 

location.protocol;
location.replace();

location.reload();

location.search;

Array.bind();

Array.prototype.concat();

Array.name.slice();

Array.prototype.copyWithin();

Array.name.split();

Array.name.substring();

Array.name.substr();

Array.name.toLowerCase();

Array.name.toUpperCase();

names.filter(function (name) {
   return name.length > 3;
}
)

names.map(function (name) {
   return name.length;
})

names.push('hello');

names.sort(function (a, b) {
   return a.length - b.length;
})

names.splice(1, 1);

names.concat(['hello', 'world']);

names.slice(1, 3);

names.join(' ');

names.pop();

names.shift();

names.unshift('hello');

names.reverse();

names.indexOf('hello');

names.lastIndexOf('hello');

names.every(function (name) {
   return name.length > 3;
}
)

names.some(function (name) {
   return name.length > 3;
}
)

names.reduce(function (previousValue, currentValue) {
   return previousValue + currentValue;
}
)  
names.reduceRight(function (previousValue, currentValue) {
   return previousValue + currentValue;
}
)

names.forEach(function (name) {
   console.log(name);
}
)

history.pushState({}, '', '/hello');

history.replaceState({}, '', '/hello');

history.back();

history.forward();

history.go();

history.length;

function tag(strings, ...values) {
   console.log(strings);
   console.log(values);
}

tag`hello ${name} world`;

var str = 'hello world';

str.startsWith('hello');

str.endsWith('world');

str.includes('hello');

str.repeat(3);

str.trim();

str.trimLeft();

str.trimRight();

str.codePointAt(0);

str.normalize();

str.charAt(0);

str.charCodeAt(0);

str.concat('hello');

str.indexOf('hello');

str.lastIndexOf('hello');

str.localeCompare('hello');

str.match(/hello/);

string.raw['hello'];

str.replace(/hello/, 'world');

str.search(/hello/);

str.slice(0, 3);

string.split(' ');

str.substring(0, 3);

str.toLowerCase();

str.toUpperCase();

str.valueOf();
string.concat('hello');

string.charAt(0);

"hello".charAt(0);

"hello".charCodeAt(0);

"hello".concat('world');

"hello".indexOf('world');

"hello".lastIndexOf('world');

"hello".localeCompare('world');
string.indexOf('hello');

string.big();

string.blink();

string.bold();

string.fixed();

string.fontcolor('red');

string.fontsize(12);

string.italics();

string.link('http://www.baidu.com');

string.small();

string.strike();

string.sub();

string.sup();

string.anchor('hello');

string.toLowerCase();

string.toUpperCase();

string.valueOf();

string.concat('hello');

string.anchor('hello');

addEventListener('click', function () {
   console.log('hello');
})

removeEventListener('click', function () {
   console.log('hello');
})

AudioDestinationNode.prototype.addEventListener('click', function () {
   console.log('hello');
}
   
)

AudioDestinationNode.prototype.removeEventListener('click', function () {
   console.log('hello');
})

window.self;
window.stop();

window.setTimeout(function () {
   console.log('hello');
}, 1000)

window.length;

window.name;

window.open('http://www.baidu.com');

window.opener;

window.parent;

window.postMessage('hello', 'http://www.baidu.com');

window.print();

window.prompt('hello');

window.requestAnimationFrame(function () {
   console.log('hello');
}
)

window.resizeBy(100, 100);

window.resizeTo(100, 100);

window.scrollBy(100, 100);

window.scrollTo(100, 100);

window.AbortController();

window.AbortSignal();

alert('hello');

confirm('hello');

prompt('hello');

window.open('http://www.baidu.com');

window.print();

window.innerHeight;
window.innerWidth;

handleEvent = function (event) {
   console.log(event);
}

addEventListener('click', this);

removeEventListener('click', this);

addEventListener('click', function () {
   console.log('hello');
}
)

removeEventListener('click', function () {
   console.log('hello');
}
)

addEventListener('click', function () {
   console.log('hello');
}
)

window.setInterval(function () {
   console.log('hello');
}
   , 1000)

window.setTimeout(function () {
   console.log('hello');
}
   , 1000)

window.clearInterval();

window.clearTimeout();

window.requestAnimationFrame(function () {
   console.log('hello');
}
)

window.cancelAnimationFrame();

window.requestIdleCallback(function () {
   console.log('hello');
}
   
)

Math.LN2;

Math.LN10;

Math.LOG2E;

String.fromCharCode(97);
String.raw['hello'];
String.fromCodePoint(97);

var str = 'hello world';

str.charAt(0);

window.forward();

window.history;
window.back();

window.history.back();

window.localStorage();

window.sessionStorage();

PictureInPictureWindow.prototype.addEventListener('click', function () {
   console.log('hello');
}
)
window.openDate();

window.openDatabase();

window.openDialog();

window.openFile();

window.openFolder();

window.openLocation();

window.openPreferences();

window.openSearch();

window.openURI();

window.openURIWithFlags();

window.openWebPanel();

window.openWindow();

window.openWindowWithFlags();

window.openWindowWithPrefs();

window.openWindowWithPrefsAndArgs();

window.openWindowWithPrefsAndArgsAndFlags();

window.






