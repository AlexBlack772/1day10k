import chai from 'chai';
import ethers from 'ethers';

describe('1201/crud/crud.js', async function () {
   it('should import ethers', function () {
      chai.expect(ethers).to.not.be.undefined;
   });
   it('should import react', function () {
      chai.expect(react).to.not.be.undefined;
   });
   it('should import useState', function () {
      chai.expect(useState).to.not.be.undefined;
   });
   it('should import useEffect', function () {
      chai.expect(useEffect).to.not.be.undefined;
   });

})