
let mut price = 0;
let mut drink 

trait Machine {
    fn buy(price: i32) -> i32;
}

struct drink {
    name: String,
    price: i32,
}

struct Person {
    name: String,
    age: i32,
    thing: str[],
    money: i32,
}

fn buy (price: i32) -> i32 {
   if Person.money < price {
       println!("You don't have enough money");
   } else {
         Person.money = Person.money - price;
         Person.thing = Person.thing + drink.name;
   }
}

fn plusOne(price: i32) -> i32 {
    Person.thing = Person.thing + drink.name;
}

fn 
